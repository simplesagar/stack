type: 'application'
language: 'go'
project:
  name: 'auth'
  description: 'Auth service'
tasks:
  build:
    command: 'go build -o build/auth ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'
