type: 'application'
language: 'go'
project:
  name: 'gateway'
  description: 'Gateway service'
tasks:
  build:
    command: 'go build -o build/gateway ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'
