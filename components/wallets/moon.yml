type: 'tool'
language: 'go'

tasks:
  build:
    command: 'go build -o build/wallets ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'
  lint:
    command: 'golangci-lint run --fix'
    inputs:
      - '**/*'