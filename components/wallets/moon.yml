type: 'application'
language: 'go'
project:
  name: 'wallets'
  description: 'Wallets Service'
tasks:
  build:
    command: 'go build -o build/wallets ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'
