type: 'tool'
language: 'go'

tasks:
  build:
    command: 'go build -o build/orchestration ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'
  lint:
    command: 'golangci-lint run --fix'
    inputs:
      - '**/*'