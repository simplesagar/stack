type: 'application'
language: 'go'
project:
  name: 'orchestration'
  description: 'Orchestration service'
tasks:
  build:
    command: 'go build -o build/orchestration ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'
