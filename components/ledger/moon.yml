type: 'application'
language: 'go'
project:
  name: 'ledger'
  description: 'Ledger is a simple, fast, and secure way to manage your finances.'
dependsOn:
  - machine

tasks:
  build:
    command: 'go build -o build/ledger ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'