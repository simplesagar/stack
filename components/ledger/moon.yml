type: 'tool'
language: 'go'

dependsOn:
  - machine

tasks:
  build:
    command: 'go build -o build/ledger ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'
  lint:
    command: 'golangci-lint run --fix'
    inputs:
      - '**/*'