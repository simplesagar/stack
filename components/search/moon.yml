type: 'application'
language: 'go'
project:
  name: 'search'
  description: 'Search component'
tasks:
  build:
    command: 'go build -o build/search ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'
