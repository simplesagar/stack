type: 'application'
language: 'go'
project:
  name: 'payments'
  description: 'Payments service'
tasks:
  build:
    command: 'go build -o build/payments ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'