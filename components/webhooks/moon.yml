type: 'application'
language: 'go'
project:
  name: 'webhooks'
  description: 'Webhooks Service'
tasks:
  build:
    command: 'go build -o build/webhooks ./'
    inputs:
      - '**/*'
    outputs:
      - 'build'